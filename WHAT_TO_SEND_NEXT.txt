WHAT TO SEND NEXT (so I can refine your portfolio)
=================================================

Fill in anything you have below and paste it in chat or attach files. Mark "N/A" if not applicable.

1) VISUALS
   - Professional headshot (PNG/JPG, high resolution).
   - Real screenshots for each project (replace SVGs in /public/projects/):
       • premier-product.* 
       • aws-3tier.*
       • codequest.*
       • codequest-backend.*
       • ai-social.*
   - Optional: logo mark if you want it in the navbar instead of text.

2) CERTIFICATE PROOF (optional but nice)
   - For each certificate in data/career.json: credential ID, completion date, or PDF/image link.
   - Issuer names if you want them more exact than the current placeholders.

3) PROJECT LINKS
   - GitHub repo URLs per project (if public), not only profile link.
   - Backend URL for CodeQuest (Render) if you want it shown explicitly.
   - AI Social Media Post Generator live demo URL (if deployed).

4) AWS 3-TIER PROJECT
   - Architecture diagram image (the ones referenced in your notes).
   - Short "lessons learned" paragraph you want quoted verbatim.

5) CAREER / RESUME SIGNALS
   - Current role / education line you want under your name.
   - 3–6 bullet "impact" statements with metrics (latency, users, cost, uptime).
   - Technologies you want emphasized that are missing from Skills.

6) CONTACT FORM DESTINATION
   - Preferred: Resend / SendGrid API key + target email (I can wire server email).
   - Or webhook URL (Slack / Discord / Form backend).

7) LEGAL & PRIVACY
   - Whether contact submissions may be stored on disk in production (currently JSONL for dev).
   - Cookie / analytics preference (Plausible, GA4, none).

8) DEPLOYMENT
   - Domain you will use (for metadata / canonical URLs).
   - Hosting target (Vercel assumed) and any environment variables you already use.

When you send updates, mention which sections should change: Hero, About, Projects, Certifications, Skills, Contact, or Footer.
